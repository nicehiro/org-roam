#+title: Python Scope Resolution
#+roam_alias: 作用域范围
#+roam_tags:

作用域是 Python 程序可以直接访问[[file:20210221095523-python_namespaces.org][命名空间]]的正文区域。

在一个 Python 程序中，直接访问一个变量，会从内到外依次访问所有的作用域，
直到找到那个变量，否则就会报未定义的错误。
